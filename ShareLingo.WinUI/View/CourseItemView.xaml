<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ShareLingo.WinUI.View.CourseItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShareLingo.WinUI.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ShareLingo.WinUI.ViewModel"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance vm:CourseContainerViewModel, IsDesignTimeCreatable=False}">
    <Border Background="{StaticResource ButtonBackgroundThemeBrush}" 
            PointerEntered="Border_PointerEntered"
            PointerExited="Border_PointerExited">
        <Grid x:Name="RootElement">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Width="150" Height="150" CornerRadius="10">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#ff1b6b" Offset="0.0" />
                        <GradientStop Color="#45caff" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>
                <Viewbox Grid.Row="0" Width="80">
                    <SymbolIcon Symbol="Pictures" Opacity="0.6"/>
                </Viewbox>
            </Border>
            <Border x:Name="GradientBorder" Grid.Row="0" CornerRadius="10" Opacity="0">
                <Border.Background>
                    <RadialGradientBrush>
                        <GradientStop Offset="0" Color="#0fff"/>
                        <GradientStop Offset="1" Color="#3000"/>
                    </RadialGradientBrush>
                </Border.Background>
            </Border>
            <TextBlock Grid.Row="1" HorizontalAlignment="Left" TextAlignment="Center" Margin="3,6,0,6" 
                   FontWeight="Medium" Opacity="1" FontSize="14" Foreground="#fff"
                   Text="{Binding Name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Button x:Name="CourseMenuButton" Background="Transparent" BorderBrush="Transparent" Visibility="Collapsed"
                    VerticalAlignment="Top" HorizontalAlignment="Right" Margin="5" Padding="3">
                <SymbolIcon Symbol="More"/>
                <Button.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedLeft">
                        <MenuFlyoutItem Text="Delete"/>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
        </Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonState">
                <VisualState x:Name="PointerOver">
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="GradientPart" Storyboard.TargetProperty="GradientStop.Offset" 
                                         To="0" Duration="0:0:0.1"/>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Border>
</UserControl>
