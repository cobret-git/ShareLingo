<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="SidebarNavigationButtonStyle" TargetType="ToggleButton">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="CheckedBorder" Grid.Column="0" Width="4" CornerRadius="3" Height="22"
                                Visibility="Visible" Opacity="0"
                                VerticalAlignment="Center" Background="{DynamicResource BorderColorAccentEmphasisBrush}"/>
                        <Border x:Name="Border" Grid.Column="1" VerticalAlignment="Stretch"
                                Margin="4,0,0,0"
                                Background="Transparent">
                            <Grid>
                                <ContentPresenter x:Name="ContentPresenter" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                              RecognizesAccessKey="True" Margin="{TemplateBinding Padding}"/>
                                <Border x:Name="CheckedStatesGrid" CornerRadius="6" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="Transparent"/>
                                <Border x:Name="CommonStatesGrid" CornerRadius="6" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="Transparent"/>
                            </Grid>
                        </Border>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Panel.Background).(Color)"
                                                        Storyboard.TargetName="CommonStatesGrid" Duration="0:0:0.12"
                                                        To="{DynamicResource ControlTransparentBgColorHover}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Panel.Background).(Color)"
                                                        Storyboard.TargetName="CommonStatesGrid" Duration="0:0:0.03"
                                                        To="{DynamicResource ControlTransparentBgColorActive}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(TextBlock.Foreground).(Color)"
                                                        Storyboard.TargetName="ContentPresenter" Duration="0:0:0.03"
                                                        To="{DynamicResource FgColorDisabled}"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(Panel.Opacity)"
                                                        Storyboard.TargetName="CommonStatesGrid" Duration="0:0:0.03"
                                                        To="0.4"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(Panel.Opacity)"
                                                        Storyboard.TargetName="CheckedStatesGrid" Duration="0:0:0.03"
                                                        To="0.4"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(Panel.Opacity)"
                                                        Storyboard.TargetName="CheckedBorder" Duration="0:0:0.03"
                                                        To="0.4"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CheckStates">
                                <VisualState x:Name="Checked">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(Panel.Opacity)"
                                                         Storyboard.TargetName="CheckedBorder" Duration="0:0:0.1"
                                                         To="1"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Panel.Background).(Color)"
                                                        Storyboard.TargetName="CheckedStatesGrid" Duration="0:0:0.12"
                                                        To="{DynamicResource ControlTransparentBgColorSelected}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Unchecked">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(Panel.Opacity)"
                                                         Storyboard.TargetName="CheckedBorder" Duration="0:0:0.1"
                                                         To="0"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Panel.Background).(Color)"
                                                        Storyboard.TargetName="CheckedStatesGrid" Duration="0:0:0.12"
                                                        To="Transparent"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ContentPresenter" Property="TextBlock.FontWeight" Value="Bold"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="ContentPresenter" Property="TextBlock.FontWeight" Value="Normal"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="SidebarNavigationTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Foreground" Value="{StaticResource FgColorDefaultBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="LineHeight" Value="1.48"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Margin" Value="5,0,0,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style x:Key="SidebarNavigationIconStyle" TargetType="ContentControl">
        <Setter Property="Width" Value="22"/>
        <Setter Property="Height" Value="22"/>
        <Setter Property="Foreground" Value="{StaticResource FgColorMutedBrush}"/>
    </Style>
    <Style x:Key="ApplicationTitleTextBlockStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource FgColorDefaultBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style x:Key="PageH1TextBlockStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource FgColorDefaultBrush}"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style x:Key="PageH2TextBlockStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource FgColorDefaultBrush}"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style x:Key="ButtonDefaultStyle" TargetType="Button">
        <Setter Property="Padding" Value="12,3"/>
        <Setter Property="Background" Value="{StaticResource ButtonDefaultBgColorRestBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonDefaultBorderColorRestBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonDefaultFgColorRestBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border TextBlock.Foreground="{TemplateBinding Foreground}" 
                            x:Name="Border"
                            CornerRadius="6"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter Margin="2"
                                          x:Name="ContentPresenter"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            RecognizesAccessKey="True" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                        To="{StaticResource ButtonDefaultBgColorHover}"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                        To="{StaticResource ButtonDefaultBorderColorHover}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                        To="{StaticResource ButtonDefaultBgColorActive}"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                        To="{StaticResource ButtonDefaultBorderColorActive}"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                        To="{StaticResource ButtonDefaultBgColorDisabled}"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        Storyboard.TargetName="Border" Duration="0:0:0.04"
                                                        To="{StaticResource ButtonDefaultBorderColorDisabled}"/>
                                        <DoubleAnimation Storyboard.TargetProperty="ContentPresenter.Opacity"
                                                        Storyboard.TargetName="ContentPresenter" Duration="0:0:0.04"
                                                        To="0.5"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="AddCourseModuleButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonDefaultStyle}">
        <Setter Property="Width" Value="180"/>
        <Setter Property="Height" Value="180"/>
        <Setter Property="Margin" Value="0,0,10,0"/>
        
    </Style>
    <Style x:Key="AddCourseModuleIconStyle" TargetType="ContentControl">
        <Setter Property="Width" Value="80"/>
        <Setter Property="Height" Value="80"/>
        <Setter Property="Foreground" Value="{DynamicResource FgColorMutedBrush}"/>
    </Style>
    <Style x:Key="AddCourseModuleTextStyle" TargetType="TextBlock">
        <Setter Property="TextBlock.FontWeight" Value="Medium"/>
        <Setter Property="TextBlock.FontSize" Value="18"/>
        <Setter Property="TextBlock.TextWrapping" Value="Wrap"/>
        <Setter Property="Margin" Value="0,6,0,0"/>
        <Setter Property="MaxWidth" Value="160"/>
    </Style>
    <Style x:Key="EditTextBoxLabelStyle" TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource FgColorDefaultBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
        <Setter Property="Padding" Value="0"/>
    </Style>
    <Style x:Key="TextBoxDefaultStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource BgColorMutedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColorDefaultBrush}"/>
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">
                    <Border Name="Border" CornerRadius="6" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <ScrollViewer Margin="{TemplateBinding Padding}" x:Name="PART_ContentHost" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="EditorTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource TextBoxDefaultStyle}">
        <Setter Property="Margin" Value="0,0,0,6"/>
        <Setter Property="Width" Value="600"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>
</ResourceDictionary>